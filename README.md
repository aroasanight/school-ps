This repo contains misc files helpful for changing settings on PC's at my (former) secondary school.

`ae.exe` is a copy of the old windows 10 powershell executable, which still runs after udpating to win11 since the new powershell was blocked by file hash but not the old one

`darkmode.txt` contains misc useful commands for various actions such as dark mode

I really don't know why `userChrome.css` is in here, this used to be my firefox usercheome but not anymore. I have a new repo for that
